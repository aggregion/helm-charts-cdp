{{- if .Values.watcher.enabled }}
{{- $serviceName := include "pipeline.watcher.fullname" . }}

{{- if .Values.watcher.probes.livenessEnabled }}
{{- $component := "pipeline-watcher" }}
apiVersion: v1
kind: Service
metadata:
  name: "hc-{{ $serviceName | lower }}"
  annotations:
    prometheus.io/healthcheck: "true"
    prometheus.io/probe: "true"
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.watcher.probes.healthcheckPort }}
      targetPort: {{ .Values.watcher.probes.healthcheckPort }}
  selector:
    app.kubernetes.io/component: "{{ $component }}"
    app.kubernetes.io/name: "{{ $serviceName }}"
  type: ClusterIP
---
{{- end -}}

{{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "registry-nginx-proxy.fullname" . }}
  labels:
    {{- include "registry-nginx-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "registry-nginx-proxy.selectorLabels" . | nindent 4 }}

{{- if .Values.emailservice.enabled }}
{{- $serviceName := include "cdp.emailservice.fullname" . }}

{{- if .Values.emailservice.probes.livenessEnabled }}
{{- $component := "email-service" }}
apiVersion: v1
kind: Service
metadata:
  name: "hc-{{ $serviceName | lower }}"
  annotations:
    prometheus.io/healthcheck: "true"
    prometheus.io/probe: "true"
  labels:
    operated-prometheus: "true"
spec:
  ports:
    - protocol: TCP
      name: {{ $component | quote }}
      port: {{ .Values.emailservice.probes.healthcheckPort }}
      targetPort: {{ .Values.emailservice.probes.healthcheckPort }}
  selector:
    {{- include "cdp.emailservice.selectorLabels" . | nindent 4 }}
  type: ClusterIP
---
{{- end -}}

{{- end }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-load-balancer.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-load-balancer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-load-balancer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

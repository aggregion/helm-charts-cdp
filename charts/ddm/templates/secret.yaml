{{- $name := include "ddm.fullname" . -}}

apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}-secrets
  labels:
    {{- include "ddm.labels" . | nindent 4 }}
type: Opaque
stringData:
  wallet.yaml: |
    {{- .Values.wallet | toYaml | nindent 4 }}
